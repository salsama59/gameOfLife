package com.internal.gameoflife.enums;

/**
 * This enum represent the cell state, the cell can be ALIVE or DEAD
 * The enum attribute ordinal is use for the application needs, so DEAD => 0 and ALIVE => 1
 * @author syeponde
 */
public enum GridCellState {DEAD, ALIVE}
